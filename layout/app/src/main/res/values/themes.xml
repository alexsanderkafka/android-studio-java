<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Layout1" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorAccent">@color/switchs</item>
        <item name="android:windowBackground">@color/backgrounds</item>
        <item name="android:textColor">@color/corText</item>
        <item name="android:textColorPrimary">@color/corTextTitle</item>
    </style>

    <style name="Theme.Layout1" parent="Base.Theme.Layout1" />
</resources>